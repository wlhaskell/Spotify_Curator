<h1>Create a new playlist!</h1>

<%= form_for(@playlist) do |f| %>
  <% if @playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
      <% @playlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :danceability_low %><br>
    <%= f.text_field :danceability_low %>
  </div>
  <div class="field">
    <%= f.label :danceability_high %><br>
    <%= f.text_field :danceability_high %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
