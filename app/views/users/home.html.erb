	

	<table class='table'>
		<tbody>
		<%if @playlists != nil %>
			<% @playlists.each do |playlist| %>
					<tr>
						<td> <%= playlist.name %></td>
					</tr>
			<% end %>
		<% end %>
		</tbody>
	</table>

	<%= link_to 'New Playlist', new_playlist_path(:id => @user.id)  %>
